import { functionName } from './{{dashCase name}}.{{lowerCase type}}';

describe('', () => {
    it('returns true', () => {
        const result = functionName();
        expect(result).toBe(true);
    });
});
